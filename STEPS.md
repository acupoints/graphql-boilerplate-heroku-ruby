## STEPS

```bash
npx create-next-app graphql-boilerplate
yarn add apollo-server-micro
yarn add axios
## The port in the environment variable $PORT must be specified in the Procfile to be recognized

```